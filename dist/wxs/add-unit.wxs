var REGEXP = getRegExp('^-?\x5cx5cd+(\x5cx5c.\x5cx5cd+)?$');

function addUnit(value) {
    if (value == null) {
        return ((undefined))
    };
    return ((REGEXP.test('' + value) ? value + 'px' : value))
};
module.exports = addUnit;